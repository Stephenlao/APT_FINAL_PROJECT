#include <fstream>
#include <sstream>
#include "Request.h"

Request::Request(string hostName_val, string supporterName, string skill, string status)
    : userId(userId_val), password(password_val){};
